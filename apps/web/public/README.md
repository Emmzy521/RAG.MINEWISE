# Public Assets

Place static assets here that should be served from the root URL.

## Background Image

Place your mine tunnel background image here as `mine-tunnel-bg.jpg`.

The image will be automatically served at `/mine-tunnel-bg.jpg` and used as the login page background.

Supported formats: `.jpg`, `.jpeg`, `.png`, `.webp`

